[build]
builder = "nixpacks"

[deploy]
startCommand = "bash -c 'source railway-migrate.sh && exec php artisan serve --host=0.0.0.0 --port=${PORT}'"
healthcheckPath = "/health.php"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"